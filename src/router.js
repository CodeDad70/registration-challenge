import React from "react";
import { BrowserRouter } from "react-router-dom";

const Router = function Router() {
  //Put your routes inside the BrowserRouter
  return <BrowserRouter />;
};

export default Router;
